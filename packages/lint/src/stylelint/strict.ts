/**
 * Strict Stylelint configuration
 * BEM naming + stricter rules
 */

import type { StylelintConfig } from './types';

export const strict: StylelintConfig = {
  rules: {
    // TODO: M4 - Implement strict mode rules
  },
};
